import { AuthCard } from '@/components/auth/AuthCard'

export const metadata = { title: 'Reset password — JustDoIt' }

export default function ForgotPasswordPage() {
  return <AuthCard initialMode="forgot" />
}
