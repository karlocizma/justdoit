import { AuthCard } from '@/components/auth/AuthCard'

export const metadata = { title: 'Create account — JustDoIt' }

export default function RegisterPage() {
  return <AuthCard initialMode="register" />
}
